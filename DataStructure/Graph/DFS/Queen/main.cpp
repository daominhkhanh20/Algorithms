#include<iostream>
using namespace std;
#define N 8

int a[N][N] = { 0 };


void Find(int x, int y) {
	a[x][y] = 1;
	for (int i = 0; i < N; i++) {
		for (int j = 0; j < N; j++) {
			if(i!=x&&j!=y&&)
		}
	}
}